{
  "name": "agent-engine",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@11.6.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "agent": "turbo run build --output-logs=errors-only && pnpm --filter @arzonic/agent-cli run agent",
    "api": "turbo run build --output-logs=errors-only && pnpm --filter @arzonic/agent-api run start"
  },
  "devDependencies": {
    "@types/node": "25.9.3",
    "turbo": "2.9.18",
    "typescript": "5.9.3"
  }
}
